import { PropertyBase } from "./property-base";
/**
 * A property representing an array item (e.g. an array of Address objects)
 */
export declare class ArrayItemProperty<T> extends PropertyBase<T> {
}
